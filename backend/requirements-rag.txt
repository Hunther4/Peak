# Peak — RAG Dependencies (opcional)
# Instalar solo si querés indexar/consultar PDFs locales:
#   pip install -r requirements-rag.txt
#
# Esto instala llama-index-core (sin torch) + embeddings HuggingFace.
# Si necesitás GPU, agregá torch aparte.

llama-index-core~=0.12.16
llama-index-embeddings-huggingface~=0.5.1
llama-index-vector-stores-chroma~=0.3.1
