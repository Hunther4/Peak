/**
 * Peak app version constant. Update this on every release.
 *
 * Format: semver MAJOR.MINOR.PATCH
 *   - MAJOR: breaking UX or data model changes
 *   - MINOR: new feature or skill shipped to users
 *   - PATCH: bug fixes, polish, internal refactors
 *
 * Also update the corresponding `version` field in frontend/package.json
 * so dependency managers reflect the same number.
 */
export const APP_VERSION = "1.2.0"
export const APP_NAME = "Peak Practice"
export const APP_TAGLINE = "Práctica Deliberada"
